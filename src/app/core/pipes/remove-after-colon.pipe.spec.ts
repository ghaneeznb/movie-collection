import { RemoveAfterColonPipe } from './remove-after-colon.pipe';

describe('RemoveAfterColonPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveAfterColonPipe();
    expect(pipe).toBeTruthy();
  });
});
