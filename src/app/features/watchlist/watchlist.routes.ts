import { Route } from '@angular/router';
import { WatchlistComponent } from './watchlist/watchlist.component';

export const WATCHLIST_ROUTES: Route[] = [
  { path: '', component: WatchlistComponent },
];